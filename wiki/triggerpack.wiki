#summary A trigger pack is a package of commands for momobot

= Introduction =

Triggers are dynamically load, so using only required triggered packs may optimize your bot and avoid unwanted commands to exist.


= List of triggerpacks =
Click on a pack to get more information about it the list of commands it contains.
  * *[pack_admin admin]* : contains commands to control the behavior of the bot
  * *[PackBase base]* : contains basic useful commands 
  * *[pack_event event]* : contains a few commands that are common to momobot's event
  * *[pack_gather gather]* : should contain everything you need to start a gather
  * *[pack_pendu pendu]* : contains commands to play a game of _pendu_
  * *[pack_fun fun]* : contains hilarious commands.
  * *[pack_websearch websearch]*: contains commands to query search engines suck as google or yahoo